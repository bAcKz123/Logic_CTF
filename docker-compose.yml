services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: logic_ctf_web
    ports:
      - "5000:80"
    volumes:
      - ./:/var/www/html
    restart: unless-stopped
